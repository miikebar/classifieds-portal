# core-firebase-firestore

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test core-firebase-firestore` to execute the unit tests via [Jest](https://jestjs.io).
